# McBeat Backend

FastAPI application. See root README for full setup instructions.

## Local Dev

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
```

## API Docs

Available at `http://localhost:8000/docs` when running locally.
